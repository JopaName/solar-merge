{
  "name": "solar-merge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run zip",
    "preview": "vite preview",
    "zip": "node -e \"const { spawn } = require('child_process'); const a = spawn('powershell', ['Compress-Archive', '-Path', 'dist/*', '-DestinationPath', 'solar-merge.zip', '-Force'], { shell: true, stdio: 'inherit' }); a.on('exit', () => console.log('solar-merge.zip created'))\""
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
